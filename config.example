# The BORG_REPO and BORG_PASSPHRASE settings are required and must be customized
#
# Absolute path to the locally installed borg command
BORG="/usr/local/bin/borg"

# =================== Customize Remote Here ========================
# REQUIRED: Set values for REMOTE_USER, REMOTE_HOST, BORG_PASSPHRASE
#
# Remote username and hostname
REMOTE_USER="<rsync.net username>"
REMOTE_HOST="<host>.rsync.net"

# Borg repository passphrase
# Alternately, set and export this in the environment
# export BORG_PASSPHRASE="longandcomplexpassphrase"
BORG_PASSPHRASE="longandcomplexpassphrase"
# ================ End Required Customization=======================

# Absolute path to the remote installed borg command
BORG_REMOTE_PATH="/usr/loca/bin/borg1/borg1"

# Storage path on remote, default: <hostname>/backups
BACK_HOST="$(hostname)"
BACK_DIR="backups"
# Remote borg repository to use
# If using a non-standard SSH port ssh:// have to be specified (per borg v1.0.9)
#BORG_REPO="ssh://${REMOTE_USER}@${REMOTE_HOST}:<ssh_port>/${BACK_HOST}/${BACK_DIR}"
BORG_REPO="${REMOTE_USER}@${REMOTE_HOST}:${BACK_HOST}/${BACK_DIR}"

PATHS=(
    "/etc"
    "/home"
    "/root"
    "/srv"
    "/usr/local"
    "/var/spool/cron/crontabs"
)
EXCLUDES=(
    "sh:/home/**/.cache"
    "/root/.cache"
    "*.pyc"
)
KEEP_HOURLY=0
KEEP_DAILY=31
KEEP_WEEKLY=0
KEEP_MONTHLY=24
KEEP_YEARLY=5

# Limit bandwith used when backing up to SSH repositories. Requires the utility `pv`.
# The unit is bytes per second. 0 means no limit. The numeric part must be an integer value.
# You can use K, M, G or T binary prefixes (power of 1024), e.g., 5M = 5MiB/s = 5242880 bytes/s.
#BWLIMIT=0

# Location of borgsync lock files
#LOCKDIR="/run/lock/borgsync"

# You can add args, such as a custom identity key file, to ssh here
#BORG_RSH="ssh"

# Optional arguments to the different borg commands.
# The current default values are listed. Modify if needed.
#BORG_CREATE_ARGS=(
#    --info
#    --stats
#    --list
#    --filter AME
#    --compression lz4
#)
#BORG_PRUNE_ARGS=(
#    --info
#    --stats
#    --list
#)
#BORG_CHECK_ARGS=(
#    --info
#)
#BORG_INIT_ARGS=(
#   --encryption repokey-blake2
#)
